<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewsX_app" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NewsX_app" parent="Base.Theme.NewsX_app" />
    <style name="AlertDialog.AppCompat.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/purple</item>
    </style>
    <style name="AlertDialog.AppCompat.Custom.Indicator2" parent="Widget.AppCompat.Button">
        <item name="android:color">@color/purple</item>
    </style>
</resources>